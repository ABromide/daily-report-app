# Schema/Data Agent Notes

This worktree owns the public data contract, deterministic sample source,
fixture generation, validation CLI, and no-network collector skeleton.

Real network collection is intentionally out of scope until the public contract,
consumer apps, and CI validation are stable.
