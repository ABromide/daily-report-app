from __future__ import annotations

from urllib.parse import urlparse

from collector.jsonio import JsonObject


def article_markdown_path(item: JsonObject) -> str:
    published_at = str(item["published_at"])
    year, month, day = published_at[:10].split("-")
    return f"articles/{year}/{month}/{day}/{item['item_id']}/index.md"


def render_article_markdown(item: JsonObject) -> str:
    title = str(item["title"])
    summary = str(item["summary_zh"])
    source_name = str(item.get("source_name", item["source_id"]))
    source_url = str(item["url"])
    category_id = str(item["category_id"])
    content_type = str(item.get("type", "blog"))
    published_at = str(item["published_at"])
    tags = [str(tag) for tag in item.get("tags", [])]
    domain = urlparse(source_url).netloc or source_name
    topic = _category_label(category_id)
    flow = _flow_for_category(category_id, tags)
    section_cards = _section_cards(title, topic, summary, tags)
    tag_text = "、".join(tags[:8]) or "未标注"
    mlpod_reference_markdown = _mlpod_reference_sections(
        title,
        topic,
        summary,
        tags,
        domain,
        source_url,
        content_type,
    )

    flow_markdown = "\n".join(
        f"{index}. **{step['title']}**：{step['body']}" for index, step in enumerate(flow, start=1)
    )
    section_markdown = "\n\n".join(f"## {card['title']}\n\n{card['body']}" for card in section_cards)

    return _strip_trailing_whitespace(
        f"""# {title}

> 研究者精读 · {topic}

**来源**：[ {source_name} ]({source_url})
**发布时间**：{published_at[:10]}
**分类**：{topic}
**标签**：{tag_text}
**来源域名**：{domain}

## TL;DR

{summary}

这一段摘要在真实自动化中不能只是价值判断。它应该直接压缩说明：原文到底研究或构建了什么，方法由哪些模块组成，训练、实验或证据如何设置，关键数字和图表结论是什么，哪些局限仍然存在。用户只看 TL;DR，就应能大概复述整篇文章的主线，而不是被引导回原文寻找细节。

## Markdown 排版要求

真实自动化写稿时要让正文容易扫读：

- 需要分点的地方必须分点，例如方法模块、实验设置、贡献、失败类型、局限和后续问题。
- 对比 benchmark、baseline、模型规模、指标数字和消融项时，优先使用 Markdown 表格。
- 需要展示数学关系时，使用行内公式 `$...$` 或块级公式 `$$...$$`。
- 需要解释系统架构、训练流程、数据流、Agent 决策链或多阶段 pipeline 时，可以画 Mermaid 图；若原文有算法，优先写清楚输入、状态、循环、判断、输出和失败边界。

```mermaid
flowchart TD
  A["读取原文和项目页"] --> B["提取模块、数据、公式和伪代码"]
  B --> C["复原作者的问题和论证顺序"]
  C --> D["用列表、表格、公式和必要图示解释"]
```

上面的 Mermaid 图只是样例；真实文章应根据原文的实际方法、训练流程或系统结构重画。

{mlpod_reference_markdown}

{section_markdown}

## 内容类型分析建议

不同来源需要不同读法：代码仓库要继续检查 docs/examples、依赖入口、核心目录、release notes 和部署说明；论文要回到 PDF 或 HTML 正文，并按背景与研究问题、方法与模型机制、训练/实验设置、主结果、消融/失败案例、Figure/Table、相关工作、最终结论与局限的顺序展开；博客/报告则按作者原文结构逐段解释，不把它硬套成论文审稿。当前内容被归入「{topic}」，所以正文应该优先回答它在这条技术链路里解决了什么问题，而不是填满固定标题。

这篇内容被归入「{topic}」，因此最重要的不是复述“它很新”，而是说明它在这条技术链路里的位置：是解决入口问题、状态管理问题、训练信号问题、评测证据问题，还是风险治理问题。真实自动化还要继续写出模块名、数据规模、训练阶段、benchmark、baseline、指标数字、消融项、失败类型、figure/table 编号、关键证据作用和局限。只有这些细节讲清楚，文章才像一篇研究分享，而不是链接摘要。

## 关键论证链

一篇合格的研究者精读必须能回答四个问题：

1. 作者先把什么现象定义成问题。
2. 接着提出了什么机制或系统来处理它。
3. 然后用什么证据证明这个机制有效。
4. 最后承认了哪些仍未解决的边界。

缺任何一环，都不能把摘要写成确定结论。本文的论证链可以这样读：问题入口来自「{summary}」；方法或系统路径由流程拆解；证据强度取决于原文是否给出代码、实验、图表、案例或官方 release；研究者判断只保留那些能被公开材料支撑的部分。真实自动化要把这些证据的具体做法写出来，而不是让读者回原文查。

## 方法或系统流程

{flow_markdown}

如果原文确实包含公式、图表、算法伪代码、指标定义或代码片段，要尽可能直接保留为 Markdown。论文类要多写公式和变量解释：原文公式优先；原文公式不足时，可以写解释性公式帮助理解目标函数、奖励、约束、指标或状态转移，但必须说明它是解释性整理。伪代码要写得像算法而不是口号：写清输入、状态、循环、分支、输出和失败边界。

## 对照与反例

这类内容最容易被过度解读。一个项目活跃更新，不等于生产可用；一篇论文提出新目标，不等于在真实训练管线里稳定；一份安全报告提出治理建议，也不等于制度已经落地。因此 Markdown 分析稿必须写出对照：它和同类方案相比新增了什么，哪些只是常规能力，哪些还缺真实证据。

如果后续自动化找不到对照材料，需要在这里显式标记，而不是把空白藏起来。研究笔记的可信度来自把“不知道”写清楚。

## 证据与边界

- **证据来源**：优先使用原文、论文页、论文 PDF、项目页、作者主页、官方博客或仓库 README；引用必须保留链接，不能只写“据称”。
- **边界条件**：如果原文缺少实验、复现代码、失败案例或安全假设，Markdown 分析稿必须明确标出，而不是用摘要掩盖。
- **后续问题**：把可复现性、部署可行性、评测覆盖、风险外溢和领域影响作为下一次研究观察项。
- **重复风险**：每次写入都要与 `known-links.json`、canonical URL、title hash 和 content hash 对比，重复候选必须换同类替代内容。
- **过度解释风险**：不能把作者没有证明的结论写成确定事实；所有扩展判断都要回到公开证据或标记为后续问题。

## 领域延伸思考

第一，检查这篇内容是否出现复现代码、作者补充说明或第三方复测；第二，检查同类项目或论文是否给出相反结果；第三，思考它对「{topic}」背后的研究路线意味着什么：它是在改变问题定义、评测方式、训练信号、安全边界，还是只是在已有框架里补一个工程细节。

这一节应该像研究者读完文章后的进一步思考：如果原文讨论后训练，就继续追问奖励、数据、泛化和评测；如果讨论 AI 安全，就继续追问威胁模型、能力阈值、治理执行和误用路径；如果讨论 Agent，就继续追问环境、动作空间、状态记忆和任务边界。

## 研究者结论

结论不需要包装成产品机会。它要回答：作者提出的问题是否重要，方法是否真正回应了这个问题，证据是否足够，哪些结论仍依赖特定数据、模型规模、评测协议或作者假设，以及它给「{topic}」领域留下了哪些更难的问题。

[打开原文]({source_url})
"""
    )


def _category_label(category_id: str) -> str:
    return {
        "llm-agent": "大模型 Agent 相关",
        "llm-post-training": "大模型后训练相关",
        "ai-safety": "AI 安全相关",
    }.get(category_id, category_id)


def _strip_trailing_whitespace(markdown: str) -> str:
    return "\n".join(line.rstrip() for line in markdown.splitlines()) + "\n"


def _flow_for_category(category_id: str, tags: list[str]) -> list[dict[str, str]]:
    if category_id == "llm-post-training":
        return [
            {"title": "定位训练阶段", "body": "先判断文章讨论的是 SFT、强化学习、OPD、蒸馏、偏好优化还是适配器训练。"},
            {"title": "拆开优化目标", "body": "继续追踪损失函数、采样策略、教师信号和学生模型更新之间的关系。"},
            {"title": "检查实验设置", "body": "把数据集、基座模型、对照组、消融实验和失败样本单独列出。"},
            {"title": "归纳工程风险", "body": "评估它在真实后训练管线里可能带来的成本、稳定性和泛化边界。"},
        ]
    if category_id == "ai-safety":
        return [
            {"title": "识别风险场景", "body": "先确认文章讨论的是网络攻防、生物安全、模型治理、越狱还是权限边界。"},
            {"title": "追踪攻击或治理链路", "body": "把风险如何出现、被放大、被检测和被处置的过程拆开。"},
            {"title": "检查证据强度", "body": "区分真实事件、红队实验、政策建议和推测性风险。"},
            {"title": "形成安全观察项", "body": "把需要长期跟踪的阈值、能力边界和治理动作写成研究问题。"},
        ]
    return [
        {"title": "识别任务入口", "body": "先判断文章里的 Agent 是解决工具调用、工作流、记忆、规划还是代码执行。"},
        {"title": "拆解系统模块", "body": f"围绕 {', '.join(tags[:3]) or '核心标签'} 追踪模型、工具、状态和用户界面的关系。"},
        {"title": "验证落地路径", "body": "检查是否有代码、产品界面、部署方式、评测案例或真实用户场景。"},
        {"title": "形成研究判断", "body": "判断它是工程组件、研究趋势、评测样例，还是只适合作为背景信号。"},
    ]


def _section_cards(title: str, topic: str, summary: str, tags: list[str]) -> list[dict[str, str]]:
    return [
        {
            "title": "文章架构拆解",
            "body": f"先把《{title}》拆成“问题入口、方法或系统、证据材料、边界结论”四段。摘要只是入口，Markdown 正文要解释每段承担什么功能，以及它为什么被放进「{topic}」。",
        },
        {
            "title": "逐部分细读",
            "body": f"第一部分确认作者要解决的问题是否足够明确；第二部分追踪它提出的机制、流程或代码结构；第三部分检查证据是否能支撑“{summary}”；第四部分把仍需追踪的问题单独列出。",
        },
        {
            "title": "标签不是结论",
            "body": f"标签「{'、'.join(tags[:5]) or '未标注'}」只帮助检索，不能替代分析。自动化必须继续解释这些标签如何对应到模型能力、训练阶段、工程模块或安全风险。",
        },
    ]


def _mlpod_reference_sections(
    title: str,
    topic: str,
    summary: str,
    tags: list[str],
    domain: str,
    source_url: str,
    content_type: str,
) -> str:
    tag_text = "、".join(tags[:5]) or "未标注"
    return f"""## 论文或项目元信息

- **标题**：{title}
- **分类**：{topic}
- **核心标签**：{tag_text}
- **原始链接**：[{domain}]({source_url})
- **写作参考**：可以参考 MLPod 论文解读长文的节奏：先给元信息和核心问题，再用长 TL;DR 概括问题、方法、证据、结论和意义；论文类必须按背景、方法、实验、结果、消融、图表、相关工作、领域延伸的顺序推进。

如果原文、论文 PDF、项目页、作者主页、Hugging Face 页面或项目 README 中存在题图、系统图、结果表格、流程图、轨迹图、截图或 benchmark 图，正文只需要解释其中真正支撑论证的关键图表：作者把问题画成了什么结构、方法在哪一步发生作用、实验结果究竟支撑了哪一个结论。图表服务于分析，正文应围绕它在论证中的作用展开。

只有当某张图或表格对理解原文不可替代时，才少量引用；如果文字、表格重写或公式已经能讲清，就以分析为主。文章的重点是精读作者的问题、方法、证据和边界。

{_type_specific_sections(content_type, title, topic, summary, tags)}"""


def _type_specific_sections(content_type: str, title: str, topic: str, summary: str, tags: list[str]) -> str:
    if content_type == "paper":
        return _paper_sections(title, topic, summary, tags)
    if content_type == "code":
        return _code_sections(title, topic, summary, tags)
    return _blog_sections(title, topic, summary, tags)


def _paper_sections(title: str, topic: str, summary: str, tags: list[str]) -> str:
    tag_text = "、".join(tags[:5]) or "未标注"
    return f"""## 1. 背景与研究问题

论文类内容首先要回答“研究空白在哪里”。自动化不能只复述摘要，而要把作者要解决的问题放回已有方法的背景里：现有方法为什么不够，模型、数据、环境或评测在什么条件下失败，作者试图把问题重新定义到哪个粒度。对《{title}》来说，首页摘要“{summary}”只是入口；真正的论文解读要继续说明这个入口对应的是训练目标、评测方法、模型结构、数据分布、执行环境还是部署风险，并尽可能写出原文给出的任务定义、数据规模、baseline 和失败场景。

这一节要写清楚研究问题的边界。如果论文讨论后训练，就要区分 SFT、RLHF、DPO、OPD、蒸馏、奖励建模和数据过滤各自处在训练链路的哪一段；如果论文讨论 Agent，就要区分规划、工具调用、记忆、执行环境、动作空间和评测；如果论文讨论 AI 安全，就要区分能力评估、威胁模型、红队方法和治理机制。只有先把问题边界放准，后面的公式、实验和图表才不会变成堆料。

## 2. 论文主张与论证路线

论文解读不应该只是按目录复述，而要先复原作者如何说服读者。建议把核心论证整理成 claim → mechanism → evidence → boundary 的表格：作者提出了什么主张，用哪个机制回应，证据来自哪张图、哪张表、哪个实验或哪个失败案例，以及这些证据仍然不能证明什么。

| 主张 | 作者用什么机制回应 | 证据来自哪里 | 仍不能证明什么 |
|---|---|---|---|
| Claim 1 | 方法模块、训练信号、评测协议或系统设计 | Figure/Table、主实验、消融或案例 | 只在某个数据集、模型规模或威胁模型下成立 |

这一节的信息粒度是“论证结构”，不是细节堆叠。它应该帮助读者知道后文为什么要讲这些方法、实验和图表，也能防止自动化把所有段落平均对待。

## 3. 方法机制：输入、状态、模块与目标函数

论文解读的方法部分要拆机制，不是摘方法名。需要说明输入是什么、状态如何表示、模型如何处理、模块之间如何连接、目标函数如何组织、输出如何被评估。每个模块都要尽可能写出具体做法：数据从哪里来、经过什么处理、模型看见哪些上下文、奖励或损失如何计算、什么时候停止、失败如何处理。还要解释每个设计的意义：它替代了什么旧做法，解决了什么失败模式，又引入了什么新假设。

## 4. 算法流程、公式与伪代码

这一节把方法变成可执行理解。看到原文公式时必须保留公式并解释变量；如果原文没有清晰伪代码，可以根据方法整理解释性伪代码，但必须说明这是解读整理。公式应该紧跟作者真正使用的目标函数、指标或状态转移。例如后训练或 Agent RL 论文常见目标可以整理成：

$$
J(\\theta)=\\mathbb{{E}}_{{x,y\\sim\\pi_\\theta}}[r(x,y)]-\\beta D_{{KL}}(\\pi_\\theta \\Vert \\pi_0)
$$

公式之后需要解释：$\\pi_\\theta$ 是被优化的策略模型，$r(x,y)$ 是奖励或偏好信号，$D_{{KL}}$ 是约束新策略不要偏离参考模型的正则项。对于网页 Agent、工具 Agent 或多轮任务，还可以用解释性状态转移公式帮助读者理解：

$$
s_{{t+1}} = T(s_t, a_t), \\quad \\tau=(s_0,a_0,r_0,\\ldots,s_T)
$$

这里 $s_t$ 是第 $t$ 步页面、上下文或环境状态，$a_t$ 是点击、输入、滚动、工具调用等动作，$T$ 是环境转移，$\\tau$ 是整条轨迹。没有解释的公式只是形式感，不能帮助用户理解论文。方法部分还要说明作者为什么这样设计：是为了降低方差、稳定训练、减少标注成本、增强泛化，还是为了暴露安全风险。

伪代码不要写成“收集数据 -> 训练模型 -> 评估结果”这种口号。更好的写法应该接近下面的结构，并根据原文替换变量和步骤：

```text
Input: task distribution D, initial model pi_0, reward/evaluator R, budget B
State: policy pi_theta, replay buffer M, validation set V
for each iteration k in 1..B:
    sample tasks x ~ D
    generate candidate trajectories y ~ pi_theta(. | x)
    score each trajectory with R and collect failure cases
    update M with (x, y, score, failure_type)
    optimize pi_theta on accepted samples with KL or safety constraints
    if validation score stops improving or safety violations increase:
        stop and report the failure mode
return pi_theta, evaluation metrics, and unresolved failure cases
```

## 5. 实验设置：数据、模型、指标与评测协议

实验设计要按 MLPod 式论文解读拆开：数据集是什么，基座模型是什么，训练设置是什么，指标如何定义，对照组和消融实验分别回答什么问题。不要只写“作者做了大量实验”。如果论文使用多个 benchmark，需要解释每个 benchmark 对应的能力维度；如果论文使用多个模型尺寸，需要说明结果是否随规模变化；如果有多阶段训练，需要说明每一阶段的输入、目标、样本量、采样策略和停止条件。凡是原文给了数字，都要尽量写进来。

这一节还要尽可能保留指标定义公式。例如成功率、平均奖励、pass@k、胜率、KL 或回报都可以用公式解释：

$$
\\text{{Success Rate}}=\\frac{{\\#\\text{{successful tasks}}}}{{\\#\\text{{all tasks}}}}, \\quad R(\\tau)=\\sum_{{t=0}}^T \\gamma^t r_t
$$

论文类内容还要关注失败案例。很多论文只在主表里展示平均提升，但真正有价值的部分往往在失败样本、泛化下降、成本增加或对照组异常里。自动化需要把这些内容从附录、表格或图注里挖出来，写成领域里仍未解决的问题。

## 6. 主结果：哪些结论被数据支持

结果部分要先按论文主表、主图或核心 benchmark 解释最终结论，再进入消融。每个结果都要回答：对比的是哪个 baseline，指标提升多少，提升是否跨数据集成立，是否只在某个模型尺寸或某个任务类型上成立。不要把结果写成“取得 SOTA”这种空话；要说明 SOTA 是在哪个 benchmark、哪个设置、哪个输入信息和哪个评测协议下成立。读者不应再需要打开原文才能知道主要结果数字。

如果原文包含表格，Markdown 里可以直接重写关键表格；如果不需要展示原图，就不要强行插入本地图片。表格解读必须说明每一列指标含义，尤其要解释成功率、准确率、pass@k、reward、judge accuracy、cost、latency 或 token budget 这类指标分别衡量什么。

## 7. 消融、失败案例与反例

消融部分要回答“哪个模块真的必要”。如果论文移除了上下文管理、奖励模型、数据过滤、KL 约束、prompt 格式、工具接口、浏览器环境或某个训练阶段，自动化要逐项说明性能如何变化，以及这个变化支持了作者的哪个主张。

失败案例和误差分析不能放在最后一笔带过。它们应该说明模型或方法在哪些场景失败：页面动态变化、长上下文遗忘、grounding 错误、奖励误判、数据分布漂移、过拟合 benchmark、计算成本过高、泛化到真实用户任务不足等。若论文没有失败案例，也要把这个缺口写清楚。

## 8. Figure/Table 逐项证据解读

这一节必须逐图逐表解释。图表不是装饰，而是论文证据的核心载体。每张图要说明：横轴纵轴是什么，曲线或柱状图比较了什么，主结果支持哪个结论，是否存在反常点，作者有没有解释这些反常点。表格要解释每一列指标的含义，不能只说“结果提升明显”。

如果原文 PDF、项目页、作者主页、Hugging Face 页面或 GitHub README 中包含 figure、table、系统图、轨迹图、截图或结果图，自动化要优先做图表解释：这张图对应论文哪一节，展示什么证据，支持什么结论，不能证明什么。只有关键图表无法用文字或表格重写时，才少量引用本地图片；不要在正文列出一串附件路径。

## 9. 相关工作与位置判断

论文不能孤立看。自动化要搜索同主题论文、作者补充、Hugging Face paper page、相关博客解读、复现讨论或 GitHub 实现。如果外部材料支持原文，要说明支持的是问题设定、方法有效性还是实验结论；如果外部材料只是背景，也要标明它不能直接证明论文结论。

对于《{title}》这种归入「{topic}」的内容，相关工作部分还要解释它和标签「{tag_text}」之间的关系。标签不能替代分析，自动化必须说明它到底补上了哪一块研究拼图。

## 10. 证据边界、局限与领域延伸

论文总结要像研究者的读书报告，但顺序要放在背景、方法、实验、结果和图表之后。这里要写：哪些结论已经被实验证明，哪些只在有限设置下成立，哪些需要复现，哪些可能受数据、模型规模、评测指标或实现细节影响。最后进一步思考它对「{topic}」的意义：如果是后训练，追问奖励信号、数据过滤和泛化；如果是 AI 安全，追问威胁模型、能力阈值和治理执行；如果是 Agent，追问环境、动作空间、记忆和评测边界。"""


def _blog_sections(title: str, topic: str, summary: str, tags: list[str]) -> str:
    tag_text = "、".join(tags[:5]) or "未标注"
    return f"""## 1. 这篇文章真正关心的问题

博客类内容的目标不是证明一个新算法，也不是复刻论文评审流程，而是尽可能详细地把作者整篇博客想讲什么讲清楚。自动化首先要说明作者为什么写这篇博客：它是在回应一个发布、一种风险、一组实践经验、一个社区争议，还是一段行业判断。对《{title}》来说，首页摘要“{summary}”只是读者入口；长文要继续展开作者的写作动机、面向读者和核心问题。

这一节要保持博客的语气和上下文。博客往往不是严格论文结构，它可能先讲故事，再讲产品细节，再转向观点；也可能先抛出结论，再逐段解释为什么。自动化要尊重这种原文结构，而不是强行套“方法、实验、消融、benchmark”。除非博客本身包含实验或 benchmark，否则不得编造论文式结果。

## 2. 作者是怎么展开这个问题的

博客分析要按作者原文顺序走一遍。第一段在铺什么背景，第二段引入了什么概念，中间哪些段落给出例子、历史背景、失败案例或对比，后半部分作者如何收束观点，这些都要写出来。这里不是机械翻译目录，而是解释每一段或每一节在论证中的功能：定义问题、制造对比、补充背景、引入证据、处理反例或收束判断。

这部分可以写成“段落地图”：开头部分讲什么，中段部分讲什么，结尾部分讲什么。对于长博客，可以把相邻段落合并成小节，但不能把不同意思的段落压成一句话。博客里出现的关键概念、产品名、角色、例子、类比、流程和作者转折，都应该被解释。

## 3. 关键段落细读

关键段落细读是博客类文章的主体。这里不是做证据链审稿，而是像认真读文章一样把作者最重要的 2 到 5 个说法讲透：这句话或段落表面在说什么，它在全文论证中承担什么作用，它隐含了什么前提或限制，以及它改变了读者对问题的什么理解。

如果博客是产品或技术博客，要解释每个功能、架构选择、用户场景和工程取舍；如果博客是研究观点，要解释作者如何从问题走到判断；如果博客是安全或政策报告，要解释作者如何组织风险、案例和建议。对「{topic}」频道来说，还要说明这篇博客具体落在哪个关注点上，而不是只贴标签「{tag_text}」。

短摘录只用于支撑关键细读。每条摘录不超过 25 个中文词或 25 个英文词，摘录后必须紧跟中文分析，说明这句话在上下文里的作用。自动化不能整段搬运博客，也不能用长引用代替自己的解释。

## 4. 案例、图表与证据的作用

博客里的案例、截图和图表通常服务于作者的叙事结构。自动化要先解释它为什么出现在这一段：是在说明一个流程、一次实验、一组用户行为、一个风险场景，还是作者用来改变读者直觉的例子。案例不能直接泛化成定律；如果作者用个案推一般规律，正文要说明这种泛化风险。只有图片本身不可替代时，才少量引用；它的论证功能比文件位置更重要。

如果博客没有图片，不需要专门补附件说明。应该把篇幅留给原文案例、概念转折和作者论证方式。图表分析的重点是帮助读者理解博客内容，而不是把图片当作视觉装饰。

## 5. 这篇文章的核心判断与边界

这一节提炼文章最值得带走的 2 到 4 个判断，并说明每个判断成立依赖什么前提、适合哪些场景、不适合哪些场景。要区分作者已经论证的内容、基于案例形成的经验判断、趋势判断，以及可以继续讨论的开放推测。不要把作者观点包装成普适定律，也不要只写优点而不写边界。

## 6. 放到 AI 安全、后训练、Agent 或对应领域里看

博客可以参考外部材料，但外部材料的作用是帮助理解原文，不是把博客改写成论文审稿。自动化可以搜索作者补充、相关论文、官方文档、第三方解读、社区讨论或相同主题的后续文章。找到外部材料后，要说明它补充了博客的哪个背景：概念来源、技术细节、争议点或后续发展。

领域延伸必须贴着原文机制，而不是发散联想。如果文章和 AI 安全有关，就继续追问对齐、评测、风险建模、能力边界、部署安全；如果和后训练有关，就继续追问偏好建模、数据质量、reward hacking、能力诱导、评测闭环；如果和 Agent 有关，就继续追问工具调用、任务分解、长程记忆、环境反馈、失败恢复、权限控制和可观测性。每个延伸点都要说明它是直接适用、机制类比，还是进一步推论。

## 7. 还值得继续追问什么

最后提出来自原文边界和未展开前提的开放问题。问题要具体：哪些需要更多案例，哪些需要数据，哪些需要系统实验，哪些判断在不同规模、不同组织或不同技术栈下可能变化。保持研究者继续阅读的姿态，不要用论文审稿口吻要求博客完成论文级证明。"""


def _code_sections(title: str, topic: str, summary: str, tags: list[str]) -> str:
    tag_text = "、".join(tags[:5]) or "未标注"
    return f"""## 1. 项目定位与使用入口

代码项目的长文要从“用户如何开始使用”写起。README、安装方式、示例命令、快速开始、demo、docs 首页和 release note 都是入口材料。对《{title}》来说，摘要“{summary}”只说明它值得看；真正的项目分析要继续回答它面向谁、解决什么任务、用户第一次运行时会经过哪些步骤。

这一节还要区分项目口号和实际入口。很多项目会说自己支持很多模型、很多任务或很多部署方式，但真正能否使用，取决于文档、示例、配置和错误处理是否完整。自动化要尽可能读 README 之外的 docs/examples，而不是只看仓库第一屏。

## 2. 工程结构

工程结构分析要看核心目录、依赖入口、配置系统、数据流和模块边界。如果是 TypeScript 项目，要看 package.json、src 目录、examples 和 build/test 配置；如果是 Python 项目，要看 pyproject、package 入口、CLI、训练或服务模块；如果是 Swift/Rust 等项目，也要看对应包管理和入口文件。

标签「{tag_text}」不能替代源码分析。自动化要解释这些标签如何落到模块里：Agent 是否有 planner、tool、memory、executor；后训练项目是否有 data、trainer、template、eval、serving；安全项目是否有 policy、scanner、sandbox、audit 或 permission 模块。

## 3. 运行流程

运行流程是代码项目最重要的部分：用户输入如何进入系统，配置在哪里被读取，状态在哪里保存，错误如何返回，日志或 tracing 如何记录，部署时需要哪些外部服务。一个项目如果声称支持 Agent 工作流，就要解释任务如何被拆解、工具如何被调用、权限如何被限制、结果如何被验证。

如果项目是后训练基础设施，需要说明数据如何加载、样本如何格式化、训练参数如何传递、LoRA 或 RLHF 模块如何接入、评测和 serving 如何形成闭环。代码项目的分析不应该像论文那样找消融，而应该看运行路径是否真实可用。

## 4. 近期变化

定时任务只收今天或本周内的内容，因此代码项目必须解释近期变化：release notes、commit、issue/discussion、文档更新或官方公告到底改了什么。一个仓库长期存在不等于本周值得写入；本周值得写入，是因为它有新版本、新模块、新修复、新文档或新的行业关注点。

这一节要把“近期变化”和“既有能力”分开。既有能力可以作为背景，近期变化才是值得精读的理由。如果没有可确认的本周更新，候选应该被丢弃或转入后续观察。

## 5. 方法价值与风险

代码项目的判断要落到方法价值：它是否提出了新的工程组织方式、评测方式、Agent 状态管理方式、训练数据处理方式或安全边界处理方式。风险也要具体：文档是否不完整，测试是否缺失，安全权限是否模糊，部署是否依赖复杂外部服务，性能是否没有数据，license 是否影响使用。

对「{topic}」频道而言，项目分析的价值在于把源码和用户场景连起来。它不是论文评测，也不是单纯 star 数判断，而是判断这个项目是否能进入真实工作流。

## 6. 领域延伸

领域延伸要列出下一步研究问题：安装试跑能验证什么假设，核心模块和同类项目相比改变了什么，issue 或 release 暴露了哪些真实边界，是否需要补充 benchmark 或安全测试。代码项目的结论应该帮助读者理解这条技术路线，而不是停在“值得关注”。"""
